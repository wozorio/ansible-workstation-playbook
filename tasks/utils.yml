---
- name: Install utils
  ansible.builtin.apt:
    name: "{{ item }}"
    state: present
  loop:
    - curl
    - git
    - htop
    - jq
    - dnsutils
    - parallel
    - python-is-python3
    - shellcheck
    - tmux
    - tree
    - unzip
    - wget
