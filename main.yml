---
- name: Set up Ubuntu 24.04
  hosts: localhost
  become: true
  gather_facts: false

  vars_files:
    - defaults/main.yml

  pre_tasks:
    - name: Perform a dist-upgrade
      ansible.builtin.apt:
        update_cache: true
        upgrade: dist
    - name: Import ZSH task
      ansible.builtin.import_tasks: tasks/zsh.yml
      tags: ["zsh"]
    - name: Import utils task
      ansible.builtin.import_tasks: tasks/utils.yml
      tags: ["utils"]

  roles:
    - role: docker
      vars:
        docker_user: "{{ user }}"
      tags: ["docker"]
    - role: homebrew
      tags: ["homebrew"]

  tasks:
    - name: Import DevOps tools task
      ansible.builtin.import_tasks: tasks/devops_tools.yml
      tags: ["devops_tools"]
    - name: Import AI agents and SKILLs task
      ansible.builtin.import_tasks: tasks/ai_agents_skills.yml
      tags: ["ai_agents_skills"]
    - name: Import stow task
      ansible.builtin.import_tasks: tasks/stow.yml
      tags: ["stow"]

  post_tasks:
    - name: Remove obsolete dependencies
      ansible.builtin.apt:
        autoremove: true
